{
  "name": "huymd-dev-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "quartz build",
    "serve": "quartz build --serve"
  },
  "dependencies": {
    "@quartz/cli": "^4.0.0"
  }
}
